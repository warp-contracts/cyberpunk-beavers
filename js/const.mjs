const EVENTS_NAME = {
  currentMove: 'currentMove',
};

const Command = {
  register: 'register',
  move: 'move'
}

const Direction = {
  left: 'left',
  right: 'right',
  up: 'up',
  down: 'down'
}

export default {
  Command, Direction, EVENTS_NAME
};

