const EVENTS_NAME = {
  currentMove: 'currentMove',
};

const Command = {
  register: 'register',
  registered: 'registered',
  move: 'move',
  moved: 'moved'
}

const Direction = {
  left: 'left',
  right: 'right',
  up: 'up',
  down: 'down'
}

export default {
  Command, Direction, EVENTS_NAME
};

